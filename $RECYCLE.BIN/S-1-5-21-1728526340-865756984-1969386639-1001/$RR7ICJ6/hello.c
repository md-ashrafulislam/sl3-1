#include <stdio.h>

int main(){

   /*  int a;

    scanf("%d", &a);
    printf("The number is: %d\n", &a);
    printf("memory size is: %d\n", sizeof(a)); */

     int A, B, X;
    scanf("%d%d", &A, &B);
    
     X = A + B;
    printf("X = %d\n", X);
     /* double r;

    scanf("%lf", &r);
    double a= PI * r* r;
    printf("A=%.4lf\n", a); 

    /* int a,b,PROD;
    scanf("%d%d", &a,&b);
    PROD= a * b;
    printf("PROD = %d", PROD); 

    /* float a,b,MEDIA;
    scanf("%f%f", &a,&b);
    MEDIA= (a+b)/2.0;
    printf("MEDIA = %.5f", MEDIA); */

    

    return 0;
} 

